# Plan: Create api/busyness.ts endpoint

## Context
Task 3 of Google Maps Busyness feature. Creating the Vercel serverless endpoint that calls the cache module from Task 2.

## Steps
1. Create `api/busyness.ts` with the provided code
2. Run `npx tsc --noEmit` to verify no type errors
3. Commit: `git add api/busyness.ts && git commit -m "feat: add /api/busyness endpoint for Google Maps busyness data"`

## Verification
- Typecheck passes with no errors
- File follows same pattern as `api/queue.ts`
